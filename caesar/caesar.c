#include <stdio.h>
#include <cs50.h>
#include <string.h>
#include <stdlib.h>

int main(int argc, string argv[]){

int i, k;

    char p[52];
    char c[52];

    string plain = get_string("Plaintext: ");
    scanf("%s\n", plain);

    //Upper Case Alphabet
    for(i=65;i<=90;i++){
        c[i]==(p[i]+argv[2])%26
    }

    //Lower Case Alphabet
    for(i=97;i<=122;i++){
        c[i]==(p[i]+argv[2])%26
    }

    printf("%i\n",ch);

}