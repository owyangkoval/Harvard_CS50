#include <stdio.h>
#include <cs50.h>
#include <string.h>
#include <stdlib.h>
#include <ctype.h>


int main(void){

string text = get_string("Text: ");
printf("%s\n",text);

int i;
char letters;
int isalpha(letters);

    //counting letters
    for(i=0;i<=strlen(text);i++)
        if (isalpha(letters))
        {
        printf("%i letters\n", isalpha);
        }
    else
        {
        return 0;
        }
}