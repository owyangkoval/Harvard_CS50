#include<stdio.h>
#include<cs50.h>
#include<string.h>

int count_letters(string text);
int count_sentences(string text);

int main(void){

int i=0;
{
    string name = get_string("Text: ");

int count_letters(string text)
    {
    for(i=0;i)
    {
        i++;
    }
    return i;
    }
}
}