#include <cs50.h>
int main(void)

{
    // Here your code !
    int x, y;

    if (x == y)
        {
            return (x + y)*3;
        }
    else (x > y || x < y);
        {
            return (x + y);
        }
}
