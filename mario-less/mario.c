#include <stdio.h>
#include <cs50.h>

int main(void){
    // Here your code !

    int x = get_int("Get X: ");
    int y = get_int("Get Y: ");
    int z = x + y;

    if (x == y)
        {
        printf("%i\n", z);
        }
    else
        {
        printf("%i\n", z);
        }
}