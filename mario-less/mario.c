#include <cs50.h>
#include <stdio.h>
#include <math.h>

int main(void)

{
    int i = get_int("Size: ");
    int row, column, space;

    //row
    for (row = 0; row < 8; row++)
    {
        //column
        for (column = 0, column < 8, column++)
            {
                for (space = 0, space < (i - 1), space++)
            }
        printf("#\n");
    }
}
