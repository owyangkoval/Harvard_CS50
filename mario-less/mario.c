#include <stdio.h>

int main(void){
    // Here your code !

    int x = get_int("Get X: ");
    int y = get_int("Get Y: ");
    int sum = x + y;

    printf("%d", x);
    printf("n\%d", y);

    if (x == y)
        {
                sum = x + y;
        }
            return sum*3;

    for (x > y || x < y)
        {
                sum = x + y;
        }
        return sum;
}
