#include <cs50.h>
#include <stdio.h>
#include <math.h>


int main(void)
{
     long num = get_long("Card Number: ");
     if ( num > 12 && num < 17 )
}
          // digits
          while ()
          {
               int dig1 = num % 10
               int num2 = (num-(num % 10))/10
               int dig2 = num2 % 10




               int num3 = (num2-(num2 % 10))/10
               int dig3 = num3 % 10
          }

     {
     }
