#include <cs50.h>
#include <stdio.h>
#include <math.h>


int main(void)

int dig, int num;
{
     num = get_long("Card Number: ");
}

          // digits
          while (num < 17 || num > 12);
          {
               int dig = num % 10
               int num2 = (num-(num % 10))/10
               int dig2 = num2 % 10
               int num3 = (num2-(num2 % 10))/10
               int dig3 = num3 % 10
          }

          for
     {
     printf("/n")
     }
