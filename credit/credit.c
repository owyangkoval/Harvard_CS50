#include <cs50.h>
#include <stdio.h>
#include <math.h>


int main(void)
{
     long num = get_long("Card Number: ");
}
          // digits
          while (dig = 0; dig < 16; dig++)

          {
               dig1 = num % 10
               process = (num-(num % 10))/10
               dig2 = num2 % 10



               int num3 = (num2-(num2 % 10))/10
               int dig3 = num3 % 10
          }

     {
     }
