#include <stdio.h>

int even(array[], int i) {
int j = 0;
int k = 0;

   if(i<=2) {
   return 2;
   }
   else if (i>2) {
   j = array[i-2]
   printf("The elements in the array are : %d, %d, %d, %d, %d, %d");
   printf("\n");
   return j;
   }
}

int main() {
scanf("Input array numbers)
   even(14);
}