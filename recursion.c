#include <stdio.h>

int fibbonacci(int previousnum, int num) {
if(i == 1) {
      return 1;
   }
else {
   int i=1;
   nextnum = previousnum + num;
   
      return j;
   }
}

int main() {
   int k;

   k = fibbonacci(55);
      printf("%d ",k);
}